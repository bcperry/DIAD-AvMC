[project]
name = "diad-amc"
version = "0.1.0"
description = "Synthetic data and branding utilities for DEVCOM AvMC DIAD customization"
requires-python = ">=3.11"
dependencies = [
    "faker>=37.1.0",
    "openpyxl>=3.1.5",
    "pillow>=10.4.0",
    "python-pptx>=1.0.2",
]

[tool.uv]
package = false
